# Meteor packages used by this project, one per line.
# Check this file (and the other files in this directory) into your repository.
#
# 'meteor add' and 'meteor remove' will edit this file for you,
# but you can also edit it by hand.

shell-server@0.2.3            # Server-side component of the `meteor shell` command.
browser-policy@1.1.0          # Configure security policies enforced by the browser

standard-minifier-js@2.0.0    # JS minifier run for production mode
es5-shim@4.6.15               # ECMAScript 5 compatibility for older browsers.
ecmascript@0.7.2              # Enable ECMAScript2015+ syntax in app code

fourseven:scss                # Style with attitude. Sass and SCSS support for Meteor.js.
seba:minifiers-autoprefixer   # Standard minifiers used with Meteor apps by default + css autoprefixer.

static-html@1.1.11            # Define static page content in .html files

practicalmeteor:mocha         # Write package tests with mocha and run them in the browser or from the command line with spacejam.
